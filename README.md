# 🚗 Audi AI Assistant

An intelligent in-car assistant using:
- LLM (OpenAI)
- RAG (custom knowledge)
- Tool calling
- Memory (user preferences)

## 🚀 Setup

```bash
git clone <repo-link>
cd audi-assistant
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt